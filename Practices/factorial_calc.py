# MR 2nd factorial calculator
# Define factorial(number)
def factorial(number):
#numbers = empty list[]
    numbers = []
    factored = number
# for i in numbers:
    for i in range(number):
# new = number -2 
        number = number - 1
        numbers.append(number)
# add new to numbers
        for i in numbers:

    
# Ask user what numbers the wpould like to factor
numbers = int(input("what number would you like to factor? "))
# if input is int
if numbers == int:
    #display [input]'s factorial is [factorial(input)]
    print([numbers]",s facotorial is" [factorial(numbers)])
#break
break
# if input is not int:
if numbers is != int:

#    input = "What number"
# factorial = number
# for i in numbers:
# factorial = factorialx = i
#return factorial